#ifndef GCD_H
#define GCD_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

char* gcdOfStrings(char* str1, char* str2);

#endif

// add both strings together (once str1 in front, and once str2) and compare them
// if its not the same, there is no solution!

// use Euclyd? (PGCD) to find the greatest common divisor